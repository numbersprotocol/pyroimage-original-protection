# TTD MVP Static Data

This directory is generated by:

```bash
npm run ttd:ingest
```

The generator reads the local `.omni/harness/` PyroImage seed artifacts and
writes sanitized data for the Tech The Dreamers MVP demo. Signed CDN URL query
strings are intentionally removed from public outputs; use the DIA API seed
refresh flow before any later image-fetch phase.
